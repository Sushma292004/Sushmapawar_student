def BubbleSort(arr):
    for i in range(0,len(arr)-1):
         for j in range(i+1,len(arr)):
              if (arr[i]>arr[j]):
                  #swap
                  arr[i],arr[j],arr[i]
    print(arr)
arr=[7,1,9,3,24,50,12,88]
BubbleSort(arr)
